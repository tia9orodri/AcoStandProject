@model IEnumerable<AcoStand.Models.Recursos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Designacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Artigo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Designacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Artigo.Contacto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRecursos">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRecursos">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRecursos">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
